
en:
  activities:
    index:
      page_header: "Activity"
      page_hint: "Overview"
    create:
      default: "%{user} created a %{model}"
    update:
      default: "%{user} updated a %{model}"
      rename: "%{user} renamed a %{model}"
      tag: "%{user} tagged a %{model}"
      move_to_context: "%{user} moved %{model}"
      move_to_content: "%{user} moved %{model}"
    delete:
      default: "%{user} deleted a %{model}"
