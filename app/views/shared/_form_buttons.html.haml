.form-actions.clearfix
  = submit_tag(submit_text, :class => "btn btn-primary", :remote => remote)
  = link_to(cancel_text, cancel_path(resource), {:class => "btn cancel", :remote => remote})
  .clearboth