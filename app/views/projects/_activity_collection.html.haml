- projects = Project.where(:id => collection.map(&:resource_id).uniq).order("UPPER(title)")

%h3= t(".header")
.activity-body
  %ul
    - projects.each do |project|
      %li= link_to_resource project
