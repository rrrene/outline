%ul.unstyled.content-items{:id => dom_id(content), :"data-sort-url" => "/#{content.holder_type.underscore.pluralize}/#{content.holder.try :id}/sort_content"}
  - content.content_items.each do |content_item|
    = render :partial => "content_items/show", :locals => {:content_item => content_item}
