!!! 5
%html
  =render themed(:head)

  %body
    %div{:class => controller_name.dasherize + '-controller'}
      %div{:class => action_name.dasherize + '-action'}
        = render themed(:navbar)
        #content-container
          = render themed(body_template)
        = render themed(:footer)

    = render "shared/quickjump_modal"
    = render "shared/new_page_modal"
    = render "shared/new_project_modal"

    #ajax-active{:style => "display: none"}
      .inner= t("helpers.ajax-active")